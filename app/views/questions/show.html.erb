<h1><%= @question.title %></h1>

  <%= link_to "Edit", edit_question_path(@question), class: 'btn btn-success' %>
  <br/>
  <%= @question.resolved ? "Resolved" : "Not Resolved"%>
  <%= check_box_tag 'resolved', 'accepted', @question.resolved, disabled: true %>

<p><%= @question.body %></p>


